What is the difference between a merge and a rebase?


Merge combines branches by creating a new merge commit, which preserves branch histories. Rebase restructures the commit history by moving one branch's changes on top of the other, which creates a linear history.


